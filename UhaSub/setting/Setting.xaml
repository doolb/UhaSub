<Window x:Class="UhaSub.setting.Setting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:p="clr-namespace:UhaSub.Properties"
        Title="Setting" Height="300" Width="300"
        WindowStyle="ToolWindow"
        Background="{Binding Source={x:Static p:UI.Default}, Path=background}"
        Foreground="{Binding Source={x:Static p:UI.Default}, Path=foreground}">
    <Grid>
        
    </Grid>
</Window>
